<div class="movie-card" style="padding-bottom: 50px;">
  <div class="movie-infos">
    <% if movie.poster_url %>
      <%= image_tag movie.poster_url %>
    <% else %>
      <p>No poster available</p>
    <% end %>
    <h2><%= movie.title %></h2>
  </div>
  <% if current_user.movies.include?(movie) %>
   <button type="button" class="btn btn-success" disabled><i class="fas fa-check"></i></button>
  <% else %>
    <%= simple_form_for @favourite_movie  do |f| %>
      <%# <%= f.association :movie, value: movie, include_hidden: true %>
      <%= f.hidden_field :movie_id, value: movie.id %>
      <%= button_tag :submit do %>
        <i class="fas fa-plus" aria-hidden="true"></i>
      <% end %>
    <% end %>
  <% end %>
</div>
